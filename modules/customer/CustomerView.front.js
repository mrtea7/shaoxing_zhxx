define(function ($, VR, Utils) {
    alert("customer")

    VR.fetch("product.price.info", {username, pwd}, function (err, data) {
        // data.success == "ok"
        window.location.reload(true);
    });

    // Utils.exec();

});